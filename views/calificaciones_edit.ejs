<%- include("partials/header") -%>
<main class="contenedor sombra">
    <section>
        <h2>Calificaciones</h2>
        <div class="container">
            <% if (message) { %>
                <p class="text-center text-danger"><%= message %></p>
                <% } %>

                <% if (calificaciones) { %>
                    <form class="add-player-form" action="" method="post" enctype="multipart/form-data">
                        <div class="form-row">
                            <div class="form-group col-md-4">
                                <label for="first-name">Calificación</label>
                                <input type="text" class="form-control" name="calificacion" id="calificacion" value="<%= calificaciones.calificacion %>" required>
                            </div>
                            <div class="form-group col-md-4">
                                <label for="last-name">Materia ID</label>
                                <input type="text" class="form-control" name="materia_id" id="materia_id" value="<%= calificaciones.materia_id %>" required>
                            </div>
                            <div class="form-group col-md-4">
                                <label for="id">Alumno ID</label>
                                <input type="text" class="form-control" name="alumno_id" id="alumno_id" value="<%= calificaciones.alumno_id %>" required disabled title="Username can't be changed">
                            </div>
                        </div>
                        <button type="submit" class="boton">Update Profesor</button>
                    </form>
                    <% } else { %>
                        <p class="text-center">Profesor Not Found. Go <a href="/calificaciones/add">here</a> to add profesor.</p>
                        <% } %>
                    </div>
    </section>
</main>
</div>
</body>
</html>
